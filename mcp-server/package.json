{
  "name": "dexter-mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server exposing one Dexter project's agent-facing state (Kanban tasks + the living dossier) as tools for a Hermes profile scoped to that project. See docs/hermes-api-spec.md.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.1",
    "zod": "^3.23.8"
  }
}
